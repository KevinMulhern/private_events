<%= link_to "Upcoming Events", user_events_path(:upcoming => true) %>
<%= link_to "past Events", user_events_path(:past => true) %>
<%= link_to "Created Events", user_events_path(:request => 'host') %>

<div class="row">
	<div class="col-sm-6 col-sm-offset-3">
		<h1>Created Events</h1>
		<% @events.each do |event| %>
			<ul>
				<li><%= event.title %></li>
			</ul>
		<% end %>
		
	</div>
	
</div>